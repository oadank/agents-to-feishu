# ⚠️ 本模板已过时（单 bot 手写配置的老流程）。
# 现在的唯一正确流程：npm run config-center → 网页 http://localhost:13600
# → 总配置建 provider/key → Agent 分配置建 bot（飞书凭证/runtime/MCP）→ 保存自动渲染 config.<bot>.env
# 桥接进程：CTI_BOT=<agentId> npm run bot
# 本文件保留仅为兼容老部署；新用户请忽略。
